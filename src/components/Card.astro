---
export interface Props {
  as: string;
}

const { as: Component = "div" } = Astro.props as Props;
---

<Component
  class:list={[Astro.props.class, "group relative flex flex-col items-start"]}
>
  <slot />
</Component>
